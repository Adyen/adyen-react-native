import { ScrollView } from 'react-native';
import { CardView } from '@adyen/react-native';

const CardForm = () => {
  return (
    <ScrollView>
      <CardView />
    </ScrollView>
  );
};

export default CardForm;