name: Collect Sonar reports
permissions:
  contents: read

on:
  workflow_call:
    inputs:
      node-version:
        required: true
        type: string

jobs:
  run-sonar:
    name: Collect Sonar reports
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v7
        with:
          node-version: ${{ inputs.node-version }}
          cache: 'yarn'

      # Swapped Homebrew for npm, which is significantly faster on Ubuntu
      - name: Install Sonar Scanner
        run: |
          npm install -g sonarqube-scanner

      - name: Download library
        uses: actions/download-artifact@v8
        with:
          name: built-lib
          path: lib

      - name: Install dependencies
        run: |
          yarn install --frozen-lockfile

      - name: Download Swift coverage report
        uses: actions/download-artifact@v8
        with:
          name: swift-coverage-report
          path: reports

      - name: Download Kotlin coverage report
        uses: actions/download-artifact@v8
        with:
          name: kotlin-coverage-report
          path: reports

      - name: Download JS coverage report
        uses: actions/download-artifact@v8
        with:
          name: js-coverage-report
          path: reports          

      - name: Run Sonar
        run: |
          sonar-scanner -Dsonar.token=${{ secrets.SONAR_TOKEN }} -X